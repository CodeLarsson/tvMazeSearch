import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 24,
  },
  headingText: {
    fontSize: 24,
    lineHeight: 32,
    fontWeight: 'bold',
  },
  screenText: {
    fontSize: 18,
    lineHeight: 21,
    letterSpacing: 0.25,
  },
});
